export interface Solution {
    id: number; //start at 0
    name: string; //what the solution is called
    description: string; //basic description of what this is doing
    price: number;
    active: boolean;
}
